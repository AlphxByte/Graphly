#include "Graphly/Application/GraphlyMainWindow.h"

using namespace Graphly;
using namespace GraphlyUI;

GraphlyMainWindow::GraphlyMainWindow(Logger* logger, const WindowSettings& settings)
: Window(settings), _logger(logger)
{

}
