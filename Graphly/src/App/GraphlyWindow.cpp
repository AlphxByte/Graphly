#include "Graphly/App/GraphlyWindow.h"

using namespace Graphly;
using namespace GraphlyUI;

GraphlyWindow::GraphlyWindow(Logger* logger, const WindowSettings& settings)
: Window(settings), _logger(logger)
{

}
